---
import Navbar from '../components/Navbar.astro';
import WorksCardVariant from '../components/WorksCardVariant.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
import Footer from '../components/Footer.astro';

const base = import.meta.env.BASE_URL || '/';

const worksList = [
  { title: '字體設計｜音樂視覺', date: '2025-04-02', image: 'images/works_10_a.svg' },
  { title: '字體設計｜音樂視覺', date: '2025-04-02', image: 'images/works_10_b.svg' },
  { title: '字體設計｜音樂視覺', date: '2025-04-02', image: 'images/works_10_c.svg' },
];
---

<BaseLayout title="10_typography_music_visual">
  <Navbar slot="navbar" />

  <div class="back-link">
    <a href={`${base}works`}>
      <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <polyline points="15 18 9 12 15 6" />
      </svg>
      返回 Works
    </a>
  </div>

  <main class="works-container">

    <!-- 上方大圖 + 文字 -->
    <section class="top-section">
      <div class="left-image">
        <WorksCardVariant 
          image={`${base}${worksList[0].image}`} 
          variant="grid"
          width="400px"
          height="400px"  
        />
      </div>
      <div class="right-text">
        <h2>{worksList[0].title}</h2>
        <p>這裡放作品介紹文字，可以多行描述作品特色或設計理念。</p>
      </div>
    </section>

    <div class="section-divider"></div>

    <!-- 下方圖片與影片網格 -->
    <section class="media-grid">
      {worksList.slice(1).map((work) => (
        <div class="media-item">
          <img src={`${base}${work.image}`} alt={work.title} />
        </div>
      ))}

      <!-- 影片卡片 + 文字旁邊 -->
      <div class="video-card-wrapper">
        <div class="video-card">
          <video autoplay muted loop playsinline>
            <source src={`${base}video/works_10_video.mp4`} type="video/mp4" />
          </video>
        </div>
        <div class="video-text">
          <h3>作品視覺影片</h3>
          <p>這裡可以放影片介紹文字，例如視覺效果、概念、設計靈感等。</p>
        </div>
      </div>

    </section>

  </main>

  <Footer slot="footer" />

  <style>
  .works-container { 
    background-color: #d3d7d91a;
    box-shadow: 0 0 16px 4px #00000033;
    padding: 40px;
    border-radius: 12px;
    max-width: 1000px;
    margin: 40px auto;
  }

  .top-section { 
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-bottom: 40px;
  }

  .right-text {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 16px;
  }
  .right-text h2 { margin: 0 0 12px; }
  .right-text p { margin: 0; line-height: 1.5; }

  .section-divider {
    width: 80%;
    height: 1px;
    background-color: rgba(255,255,255,0.4);
    margin: 40px auto;
  }

  .back-link {
    max-width: 1200px;
    margin: 20px auto 0;
    padding: 0 80px;
    font-size: 1rem;
  }
  .back-link a {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    text-decoration: none;
    color: rgba(255, 255, 255, 0.8);
    transition: color 0.2s;
  }
  .back-link a:hover { color: #E5AF3A; }
  .back-link svg { flex-shrink: 0; }

  /* 下方圖片網格 */
  .media-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 20px;
    justify-items: center;
    align-items: start;
  }

  /* 圖片與影片外框統一 */
  .media-item {
    border-radius: 12px;
    overflow: hidden;
    aspect-ratio: 1 / 1;
    background-color: rgba(0,0,0,0.1);
    max-width: 240px;
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
    transition: transform 0.3s ease;
  }

  /* 圖片/影片內容 */
  .media-item img,
  .media-item video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s ease;
  }

  /* 懸停放大 */
  .media-item:hover img,
  .media-item:hover video {
    transform: scale(1.05);
  }

  /* 影片卡片 + 文字旁邊排列 */
  .video-card-wrapper {
    display: flex;
    gap: 16px;
    max-width: 500px;
    align-items: flex-start;
    width: 100%;
  }

  /* 影片外框 */
  .video-card {
    flex: 1;
    border-radius: 12px;
    overflow: hidden;
    aspect-ratio: 1 / 1;
    cursor: pointer;
    transition: transform 0.3s ease;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* 影片內容 */
  .video-card video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s ease;
  }

  /* 懸停影片放大 */
  .video-card:hover video {
    transform: scale(1.05);
  }

  /* 影片文字 */
  .video-text {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    font-size: 0.95rem;
    line-height: 1.4;
    color: rgba(255,255,255,0.85);
  }

  /* 響應式 */
  @media (max-width: 768px) {
    .top-section { grid-template-columns: 1fr; }
    .video-card-wrapper {
      flex-direction: column;
      gap: 12px;
      max-width: 100%;
    }
    .video-text {
      max-width: 100%;
    }
  }
  </style>
</BaseLayout>
